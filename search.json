[{"title":"Hello World","url":"/2026/02/04/hello-world/","content":"Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub.\nQuick StartCreate a new post$ hexo new &quot;My New Post&quot;\n\nMore info: Writing\nRun server$ hexo server\n\nMore info: Server\nGenerate static files$ hexo generate\n\nMore info: Generating\nDeploy to remote sites$ hexo deploy\n\nMore info: Deployment\n"},{"title":"前缀和与差分：从基础到进阶的完整指南","url":"/2026/02/04/%E5%89%8D%E7%BC%80%E5%92%8C%E4%B8%8E%E5%B7%AE%E5%88%86/","content":"前缀和与差分 📊📚 前缀和基础核心概念\n定义：前缀和是指数组从第一个元素到当前位置的累加和\n用途：快速求区间和，将O(n)的区间查询优化到O(1)\n时间复杂度：\n预处理（建前缀和）：O(n)\n区间查询：O(1)\n单点修改：O(n)（需重新计算）\n\n\n空间复杂度：O(n)\n\n一维前缀和定义与公式// 前缀和数组定义// sum[i] = a[1] + a[2] + ... + a[i]// 递推公式sum[i] = sum[i-1] + a[i]// 区间和公式// sum[l,r] = a[l] + a[l+1] + ... + a[r]//          = sum[r] - sum[l-1]\n\n基础实现#include &lt;bits/stdc++.h&gt;using namespace std;const int N = 1e5 + 50;int a[N], sum[N];int n, m;// 构建前缀和数组void build_prefix_sum() &#123;    sum[0] = 0;  // 哨兵位置    for (int i = 1; i &lt;= n; i++) &#123;        sum[i] = sum[i-1] + a[i];    &#125;&#125;// 区间查询：返回[l, r]的和int range_query(int l, int r) &#123;    return sum[r] - sum[l-1];&#125;int main() &#123;    cin &gt;&gt; n &gt;&gt; m;    for (int i = 1; i &lt;= n; i++) &#123;        cin &gt;&gt; a[i];    &#125;        build_prefix_sum();        while (m--) &#123;        int l, r;        cin &gt;&gt; l &gt;&gt; r;        cout &lt;&lt; range_query(l, r) &lt;&lt; &quot;\\n&quot;;    &#125;        return 0;&#125;\n\n应用示例// 示例：求数组所有子数组的和的最大值int max_subarray_sum() &#123;    int max_sum = INT_MIN;    for (int i = 1; i &lt;= n; i++) &#123;        for (int j = i; j &lt;= n; j++) &#123;            int current_sum = sum[j] - sum[i-1];            max_sum = max(max_sum, current_sum);        &#125;    &#125;    return max_sum;&#125;\n\n二维前缀和定义与公式// 二维前缀和定义// sum[i][j] = a[1][1] + a[1][2] + ... + a[1][j]//           + a[2][1] + a[2][2] + ... + a[2][j]//           + ...//           + a[i][1] + a[i][2] + ... + a[i][j]// 递推公式sum[i][j] = sum[i-1][j] + sum[i][j-1] - sum[i-1][j-1] + a[i][j]// 矩形和公式// sum[x1,y1,x2,y2] = sum[x2][y2] - sum[x1-1][y2] - sum[x2][y1-1] + sum[x1-1][y1-1]\n\n基础实现#include &lt;bits/stdc++.h&gt;using namespace std;const int N = 1e3 + 50;int a[N][N], sum[N][N];int n, m, q;// 构建二维前缀和void build_2d_prefix() &#123;    memset(sum, 0, sizeof(sum));    for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            sum[i][j] = sum[i-1][j] + sum[i][j-1] - sum[i-1][j-1] + a[i][j];        &#125;    &#125;&#125;// 矩形查询：返回(x1,y1)到(x2,y2)的和int rect_query(int x1, int y1, int x2, int y2) &#123;    return sum[x2][y2] - sum[x1-1][y2] - sum[x2][y1-1] + sum[x1-1][y1-1];&#125;int main() &#123;    cin &gt;&gt; n &gt;&gt; m &gt;&gt; q;    for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            cin &gt;&gt; a[i][j];        &#125;    &#125;        build_2d_prefix();        while (q--) &#123;        int x1, y1, x2, y2;        cin &gt;&gt; x1 &gt;&gt; y1 &gt;&gt; x2 &gt;&gt; y2;        cout &lt;&lt; rect_query(x1, y1, x2, y2) &lt;&lt; &quot;\\n&quot;;    &#125;        return 0;&#125;\n\n可视化解释矩形区域 (x1,y1) 到 (x2,y2) 的和：    y1         y2    |          |    v          vx1 ┌──────────┐   │  目标区域 │x2 └──────────┘sum[x2][y2] = 整个大矩形- sum[x1-1][y2] = 去掉上方- sum[x2][y1-1] = 去掉左方+ sum[x1-1][y1-1] = 补回重复减去的左上角\n\n🔍 差分基础核心概念\n定义：差分是前缀和的逆运算，记录相邻元素的差值\n用途：快速进行区间修改，将O(n)的区间更新优化到O(1)\n时间复杂度：\n预处理（建差分数组）：O(n)\n区间更新：O(1)\n还原原数组：O(n)\n\n\n空间复杂度：O(n)\n\n一维差分定义与公式// 差分数组定义// diff[i] = a[i] - a[i-1]  (i &gt;= 2)// diff[1] = a[1]// 性质// a[i] = diff[1] + diff[2] + ... + diff[i] = sum[i]（差分数组的前缀和）// 区间更新// 将区间[l, r]的每个元素都加val// diff[l] += val// diff[r+1] -= val\n\n基础实现#include &lt;bits/stdc++.h&gt;using namespace std;const int N = 1e5 + 50;int a[N], diff[N];int n, m;// 构建差分数组void build_diff() &#123;    diff[1] = a[1];    for (int i = 2; i &lt;= n; i++) &#123;        diff[i] = a[i] - a[i-1];    &#125;&#125;// 区间更新：[l, r]加valvoid range_update(int l, int r, int val) &#123;    diff[l] += val;    if (r + 1 &lt;= n) &#123;        diff[r+1] -= val;    &#125;&#125;// 还原数组（计算差分数组的前缀和）void restore_array() &#123;    a[1] = diff[1];    for (int i = 2; i &lt;= n; i++) &#123;        a[i] = a[i-1] + diff[i];    &#125;&#125;int main() &#123;    cin &gt;&gt; n &gt;&gt; m;    for (int i = 1; i &lt;= n; i++) &#123;        cin &gt;&gt; a[i];    &#125;        build_diff();        while (m--) &#123;        int l, r, d;        cin &gt;&gt; l &gt;&gt; r &gt;&gt; d;        range_update(l, r, d);    &#125;        restore_array();        for (int i = 1; i &lt;= n; i++) &#123;        cout &lt;&lt; a[i] &lt;&lt; &quot; &quot;;    &#125;        return 0;&#125;\n\n区间更新的直观理解原数组：    a1  a2  a3  a4  a5差分数组：  d1  d2  d3  d4  d5  d6区间[2,4]加3：d2 += 3    ← 从位置2开始影响后续所有元素d5 -= 3    ← 从位置5开始取消影响还原后：    a1  a2+3  a3+3  a4+3  a5\n\n二维差分定义与公式// 二维差分定义// diff[i][j] 表示影响从(i,j)开始往右下扩展的区域// 区间更新// 将矩形区域(x1,y1)到(x2,y2)加val// diff[x1][y1] += val// diff[x2+1][y1] -= val// diff[x1][y2+1] -= val// diff[x2+1][y2+1] += val\n\n基础实现#include &lt;bits/stdc++.h&gt;using namespace std;const int N = 1e3 + 50;int a[N][N], diff[N][N];int n, m, q;// 区间更新：矩形区域(x1,y1)到(x2,y2)加valvoid range_update_2d(int x1, int y1, int x2, int y2, int val) &#123;    diff[x1][y1] += val;    diff[x2+1][y1] -= val;    diff[x1][y2+1] -= val;    diff[x2+1][y2+1] += val;&#125;// 还原数组（计算二维前缀和）void restore_2d() &#123;    // 先按行计算前缀和    for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            diff[i][j] += diff[i][j-1];        &#125;    &#125;    // 再按列计算前缀和    for (int j = 1; j &lt;= m; j++) &#123;        for (int i = 1; i &lt;= n; i++) &#123;            diff[i][j] += diff[i-1][j];        &#125;    &#125;    // 加到原数组上    for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            a[i][j] += diff[i][j];        &#125;    &#125;&#125;int main() &#123;    cin &gt;&gt; n &gt;&gt; m &gt;&gt; q;    for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            cin &gt;&gt; a[i][j];        &#125;    &#125;        while (q--) &#123;        int x1, y1, x2, y2, d;        cin &gt;&gt; x1 &gt;&gt; y1 &gt;&gt; x2 &gt;&gt; y2 &gt;&gt; d;        range_update_2d(x1, y1, x2, y2, d);    &#125;        restore_2d();        for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            cout &lt;&lt; a[i][j] &lt;&lt; &quot; &quot;;        &#125;        cout &lt;&lt; &quot;\\n&quot;;    &#125;        return 0;&#125;\n\n二维差分可视化矩形区域(x1,y1)到(x2,y2)加val的四个标记：    y1          y2    |           |    v           vx1  +val       -val    ↓           ↓x2  -val       +val+val: 从(x1,y1)开始往右下影响-val: 从(x2+1,y1)开始往右下取消影响-val: 从(x1,y2+1)开始往右下取消影响+val: 从(x2+1,y2+1)开始往右下恢复影响（因为被取消了两次）\n\n🚀 进阶应用1. 前缀和优化算法子数组问题// 问题：求和为k的连续子数组个数int subarray_sum_equals_k(int k) &#123;    unordered_map&lt;int, int&gt; cnt;    cnt[0] = 1;  // 前缀和为0出现1次        int prefix_sum = 0, ans = 0;    for (int i = 1; i &lt;= n; i++) &#123;        prefix_sum += a[i];        ans += cnt[prefix_sum - k];  // 统计前缀和为prefix_sum-k的次数        cnt[prefix_sum]++;    &#125;    return ans;&#125;// 问题：最长子数组，其和不超过kint max_length_subarray_sum_le_k(int k) &#123;    int prefix_sum = 0, max_len = 0;    deque&lt;int&gt; dq;  // 单调递增队列，存储前缀和的下标        dq.push_back(0);    for (int i = 1; i &lt;= n; i++) &#123;        prefix_sum += a[i];                // 移除不满足条件的元素        while (!dq.empty() &amp;&amp; prefix_sum - sum[dq.front()] &gt; k) &#123;            dq.pop_front();        &#125;                // 更新最大长度        if (!dq.empty()) &#123;            max_len = max(max_len, i - dq.front());        &#125;                // 维护单调性        while (!dq.empty() &amp;&amp; prefix_sum &lt;= sum[dq.back()]) &#123;            dq.pop_back();        &#125;        dq.push_back(i);    &#125;    return max_len;&#125;\n\n前缀和与模运算// 问题：求最长的子数组，其元素和能被k整除int max_length_divisible_by_k(int k) &#123;    unordered_map&lt;int, int&gt; first_pos;    first_pos[0] = 0;  // 前缀和模k为0的最小位置是0        int prefix_sum = 0, max_len = 0;    for (int i = 1; i &lt;= n; i++) &#123;        prefix_sum += a[i];        int mod = ((prefix_sum % k) + k) % k;  // 处理负数                if (first_pos.find(mod) == first_pos.end()) &#123;            first_pos[mod] = i;  // 第一次出现，记录位置        &#125; else &#123;            max_len = max(max_len, i - first_pos[mod]);        &#125;    &#125;    return max_len;&#125;\n\n2. 差分进阶技巧差分与前缀和结合// 问题：支持两种操作// 1. 区间[l, r]加val// 2. 查询某个位置的值// 使用差分数组 + 树状数组（或线段树）// 差分数组的树状数组实现ll tree[N];void update_diff(int pos, ll val) &#123;    for (; pos &lt;= n; pos += pos &amp; (-pos)) &#123;        tree[pos] += val;    &#125;&#125;// 区间更新void range_update_bit(int l, int r, ll val) &#123;    update_diff(l, val);    update_diff(r + 1, -val);&#125;// 单点查询ll point_query_bit(int pos) &#123;    ll sum = 0;    for (; pos &gt; 0; pos -= pos &amp; (-pos)) &#123;        sum += tree[pos];    &#125;    return sum;&#125;\n\n多次区间更新// 问题：多次区间更新后，求最终数组void multiple_range_updates() &#123;    // 所有更新操作都应用到差分数组上    for (int i = 0; i &lt; m; i++) &#123;        int l, r, val;        cin &gt;&gt; l &gt;&gt; r &gt;&gt; val;        diff[l] += val;        diff[r+1] -= val;    &#125;        // 一次性还原数组    for (int i = 1; i &lt;= n; i++) &#123;        diff[i] += diff[i-1];        a[i] = diff[i];    &#125;&#125;\n\n3. 前缀和与哈希表// 问题：给定两个数组，求最长公共子数组的长度int longest_common_subarray(vector&lt;int&gt;&amp; arr1, vector&lt;int&gt;&amp; arr2) &#123;    int n = arr1.size(), m = arr2.size();    unordered_map&lt;int, vector&lt;int&gt;&gt; pos_map;        // 记录arr2中每个值出现的位置    for (int j = 0; j &lt; m; j++) &#123;        pos_map[arr2[j]].push_back(j);    &#125;        // 计算arr1的前缀和    vector&lt;long long&gt; sum1(n + 1, 0);    for (int i = 1; i &lt;= n; i++) &#123;        sum1[i] = sum1[i-1] + arr1[i-1];    &#125;        // 计算arr2的前缀和    vector&lt;long long&gt; sum2(m + 1, 0);    for (int j = 1; j &lt;= m; j++) &#123;        sum2[j] = sum2[j-1] + arr2[j-1];    &#125;        // 检查所有可能的长度    int max_len = 0;    for (int len = min(n, m); len &gt;= 1; len--) &#123;        unordered_set&lt;long long&gt; sums;                // 存储arr1中所有长度为len的子数组和        for (int i = len; i &lt;= n; i++) &#123;            long long s = sum1[i] - sum1[i-len];            sums.insert(s);        &#125;                // 检查arr2中是否有相同的子数组和        for (int j = len; j &lt;= m; j++) &#123;            long long s = sum2[j] - sum2[j-len];            if (sums.find(s) != sums.end()) &#123;                return len;  // 找到最长的            &#125;        &#125;    &#125;        return 0;&#125;\n\n4. 二维前缀和的高级应用最大子矩阵和// 问题：求最大的子矩阵和（二维的最大子数组和）int max_submatrix_sum() &#123;    int max_sum = INT_MIN;        // 枚举上边界    for (int i = 1; i &lt;= n; i++) &#123;        int temp[N] = &#123;0&#125;;                // 枚举下边界        for (int j = i; j &lt;= n; j++) &#123;            // 将第i行到第j行压缩成一维数组            for (int k = 1; k &lt;= m; k++) &#123;                temp[k] += a[j][k];            &#125;                        // 在一维数组上求最大子数组和            int current_sum = 0, max_temp = INT_MIN;            for (int k = 1; k &lt;= m; k++) &#123;                current_sum = max(temp[k], current_sum + temp[k]);                max_temp = max(max_temp, current_sum);            &#125;                        max_sum = max(max_sum, max_temp);        &#125;    &#125;        return max_sum;&#125;\n\n统计矩阵中值为1的连通区域// 结合前缀和统计不同区域的特征void count_regions() &#123;    // 先用DFS/BFS找出所有连通区域    // 然后用前缀和快速统计每个区域的特征        vector&lt;vector&lt;int&gt;&gt; prefix_sum(n+1, vector&lt;int&gt;(m+1, 0));        for (int i = 1; i &lt;= n; i++) &#123;        for (int j = 1; j &lt;= m; j++) &#123;            prefix_sum[i][j] = prefix_sum[i-1][j] + prefix_sum[i][j-1]                              - prefix_sum[i-1][j-1] + a[i][j];        &#125;    &#125;        // 快速查询任意矩形区域的和    int query(int x1, int y1, int x2, int y2) &#123;        return prefix_sum[x2][y2] - prefix_sum[x1-1][y2]              - prefix_sum[x2][y1-1] + prefix_sum[x1-1][y1-1];    &#125;&#125;\n\n📊 复杂度对比操作复杂度对比\n\n\n操作\n暴力法\n前缀和\n差分\n\n\n\n构建数组\nO(1)\nO(n)\nO(n)\n\n\n区间查询\nO(n)\nO(1)\nO(n)\n\n\n区间更新\nO(n)\nO(n)\nO(1)\n\n\n单点修改\nO(1)\nO(n)\nO(1)\n\n\n适用场景对比\n\n\n场景\n推荐方法\n\n\n\n多次查询区间和，数组不变\n前缀和\n\n\n多次修改区间，最后查询一次\n差分\n\n\n既有查询又有修改\n树状数组&#x2F;线段树\n\n\n二维区域查询\n二维前缀和\n\n\n二维区域修改\n二维差分\n\n\n💡 实战技巧1. 边界处理// 前缀和数组多开一位，避免边界判断sum[0] = 0;for (int i = 1; i &lt;= n; i++) &#123;    sum[i] = sum[i-1] + a[i];&#125;// 差分数组多开一位，避免r+1越界void range_update(int l, int r, int val) &#123;    diff[l] += val;    if (r + 1 &lt;= n) &#123;        diff[r+1] -= val;    &#125;&#125;\n\n2. 数据范围// 注意使用long long防止溢出typedef long long ll;ll sum[N];ll a[N];\n\n3. 离散化// 当数值范围很大但元素个数较少时，使用离散化void discretize() &#123;    vector&lt;int&gt; vals;    for (int i = 1; i &lt;= n; i++) &#123;        vals.push_back(a[i]);    &#125;    sort(vals.begin(), vals.end());    vals.erase(unique(vals.begin(), vals.end()), vals.end());        // 使用离散化后的值    for (int i = 1; i &lt;= n; i++) &#123;        a[i] = lower_bound(vals.begin(), vals.end(), a[i]) - vals.begin() + 1;    &#125;&#125;\n\n🎯 典型题型1. 求区间和// P3374 【模板】树状数组1// 虽然可以用树状数组，但简单的前缀和也可以解决\n\n2. 区间加，单点查// P3368 【模板】树状数组2// 使用差分数组 + 树状数组\n\n3. 区间加，区间查// 需要两个树状数组维护差分// 或者使用线段树\n\n4. 二维区域和// P1387 最大正方形// 使用二维前缀和快速判断正方形\n\n5. 子数组问题// 和为k的子数组// 最长子数组// 连续1的个数\n\n🎓 学习建议从简单到复杂\n✅ 一维前缀和：理解基本概念和递推公式\n✅ 一维差分：理解区间更新的原理\n✅ 二维前缀和：掌握矩形区域查询\n✅ 二维差分：掌握矩形区域更新\n✅ 结合数据结构：差分+树状数组&#x2F;线段树\n✅ 高级应用：前缀和+哈希表、压缩矩阵等\n\n练习建议\n基础题：P3374、P3368\n进阶题：P1387、P2004\n难题：结合其他数据结构的前缀和问题\n\n🎊 总结前缀和的核心价值\n快速查询：O(1)时间查询任意区间和\n简单高效：代码简洁，易于实现\n扩展性强：可扩展到二维、三维\n\n差分的核心价值\n快速更新：O(1)时间更新任意区间\n批量操作：多次更新后统一还原\n空间优化：只需O(n)额外空间\n\n选择建议\n查询多，修改少 → 前缀和\n修改多，查询少 → 差分\n查询和修改都多 → 树状数组&#x2F;线段树\n二维问题 → 二维前缀和&#x2F;差分\n\n记住：前缀和和差分是互逆的运算，理解这一点有助于灵活运用它们解决问题！\n","categories":["算法笔记"],"tags":["算法","数据结构","前缀和","差分","C++"]},{"title":"我的第一篇技术文章","url":"/2026/02/04/%E6%88%91%E7%9A%84%E7%AC%AC%E4%B8%80%E7%AF%87%E6%8A%80%E6%9C%AF%E6%96%87%E7%AB%A0/","content":"搭建个人技术博客为什么要搭建个人博客在学习编程的过程中，我发现记录和分享是非常重要的。通过写博客，我可以：\n\n巩固知识：通过写作整理思路，加深理解\n建立知识体系：将零散的知识点系统化\n分享交流：与其他开发者交流经验\n长期积累：为自己的学习之路留下记录\n\n技术选型我选择了 Hexo + GitHub Pages 的方案：\n\nHexo：快速、简洁且高效的静态博客框架\nGitHub Pages：免费的静态网站托管服务\nButterfly 主题：美观、功能丰富的 Hexo 主题\n\n搭建过程整个搭建过程非常简单：\n\n安装 Node.js 和 Git\n安装 Hexo CLI\n初始化博客\n安装主题\n配置 GitHub Pages\n部署上线\n\n总共耗时约 1.5 小时，后续维护成本几乎为零。\nMarkdown 写作体验使用 Markdown 写作非常方便，支持：\n\n代码高亮：js ... \n表格：\n\n\n列1\n列2\n\n\n\nA\nB\n\n\n\n列表：有序列表和无序列表\n引用：&gt; 引用内容\n\n总结这是我博客的第一篇文章。后续我会记录学习过程中的各种技术笔记，包括但不限于：\n\n前端开发\n后端开发\n工具使用技巧\n项目实战经验\n\n如果你也想搭建个人博客，推荐从 Hexo 开始！\n\n参考资料\nHexo 官方文档\nGitHub Pages 官方文档\nButterfly 主题文档\n\n","categories":["技术笔记"],"tags":["Hexo","博客"]}]